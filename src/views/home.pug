extends base.pug
include mixins/video

block content 
    div.video-grid
        each info in videos 
            +video(info)
            +video(info)
            +video(info)
            +video(info)
            +video(info)
            +video(info)
            +video(info)
            +video(info)
            +video(info)
        else 
            span.empty__message No videos found

block script 
    script(src="/static/js/main.js")